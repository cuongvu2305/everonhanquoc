# Pages

Page components in the current no-bundler runtime are kept in `src/app/App.jsx`.
When this app is migrated to Next/Vite, split `HomePage`, `NewsPage`, `SalePage`,
`RetailPage`, `CategoryPage`, `ContactPage`, `AboutPage`, and `CheckoutPage` into this folder.
